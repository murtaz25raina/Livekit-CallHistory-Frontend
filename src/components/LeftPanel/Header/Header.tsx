import { FC } from "react";
import "./Header.css";

const Header: FC = () => {
  return (
    <header className="left-panel-header">
      <div className="left-panel-title">Chats</div>
    </header>
  );
};

export default Header;
